[Settings]
; A truth value can be true / on / 1 or false / off / 0.

; Screen resolution
screenwidth = 800
screenheight = 600

; Fullscreen
fullscreen = 0

; Use the zooming camera
zoom = on

; Default port number in network game
port = 1234

; Default server address in network game
host = localhost
